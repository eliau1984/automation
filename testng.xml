<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.22.2</version>
            <configuration>
                <includes>
                    <!-- הכנס את הטסטים החדשים -->
                    <include>**/LoginTest.class</include>
                    <include>**/LoginTest2.class</include>
                    <include>**/SeleniumTest.class</include>
                </includes>
            </configuration>
        </plugin>
    </plugins>
</build>
